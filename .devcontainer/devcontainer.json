{
  "name": "tgbot-verify Codespace",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "extensions": [
    "ms-python.python",
    "ms-python.vscode-pylance"
  ],
  "postCreateCommand": "pip install -r requirements.txt && python -m playwright install chromium && cp env.example .env || true",
  "remoteUser": "root",
  "forwardPorts": [],
  "containerEnv": {
    "MYSQL_HOST": "mysql",
    "MYSQL_PORT": "3306"
  }
}
